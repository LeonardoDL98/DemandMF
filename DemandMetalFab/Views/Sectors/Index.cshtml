@model IEnumerable<DemandMetalFab.Models.MF_Sector>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sector)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sector)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Sector }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Sector }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Sector })
        </td>
    </tr>
}

</table>
