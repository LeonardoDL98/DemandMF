<script src="~/Scripts/chartjs-plugin-datalabels.js"></script>
<script src="~/Scripts/chartjs-plugin-datalabels.min.js"></script>

<h4 class="text-center">Stacked Bar Graph by Projects </h4>
@*<input type="button" class="btn btn-primary" onclick="showgrafica()" value="Unstacked Bar Graph" />*@
<br />

<canvas id="myChart"></canvas>


<script>
    debugger
    var data= JSON.parse('@(Html.Raw(ViewBag.JsonDataSet))');

    var config = {
        type: 'bar',
        data:data

        ,
  options: {
    scales: {
      xAxes: [{
        stacked: false
      }],
      yAxes: [{
        stacked: false
      }]
    }
  }
};

var ctx = document.getElementById("myChart").getContext("2d");
    new Chart(ctx, config);


</script>